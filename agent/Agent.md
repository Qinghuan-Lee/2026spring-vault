java/python/Rag/Mcp/


# 写在agent之前

## 是什么

## 前景


**编程语言学习和基础面经**

[如何快速学习 Python](https://my.feishu.cn/docx/UE8TdEDWAoYWhPxQGmHcAeEhnSc)

> [!tips]
> 如果想做X可能需要先培养ABC能力， 你应该尽快着手去做X，并在做事的过程中迭代学习



# mcp

MCP 是 **Model Context Protocol**（模型上下文协议）的缩写，是 Anthropic 提出的一个开放标准，用于让 AI 模型（agent）与外部工具、数据源和服务进行标准化连接。

简单来说，你可以把它理解为 **AI agent 的"USB 接口"**——就像 USB 让各种设备都能通过统一接口连接电脑一样，MCP 让 AI agent 能通过统一协议连接各种外部系统。

**它要解决的核心问题：** 在 MCP 之前，每个 AI 应用要接入不同的工具（比如 Google Drive、Slack、GitHub），都需要单独写集成代码，形成"M×N"的组合爆炸问题。MCP 把这变成了"M+N"——工具端只需实现一次 MCP 服务端，AI 应用端只需实现一次 MCP 客户端，双方就能互通。

**架构上有三个角色：**

- **MCP Host（宿主）**：运行 AI agent 的应用，比如 Claude Desktop、IDE 插件等
- **MCP Client（客户端）**：宿主中负责与服务端通信的模块，维持一对一连接
- **MCP Server（服务端）**：对外暴露工具和数据的轻量服务，比如一个提供文件读写能力的 server，或一个连接数据库的 server

**MCP Server 可以向 agent 提供三类能力：**

- **Tools（工具）**：agent 可以调用的操作，比如发邮件、创建任务、查数据库
- **Resources（资源）**：agent 可以读取的数据，比如文件内容、API 返回的数据
- **Prompts（提示模板）**：预定义的交互模板，帮助用户更高效地使用特定功能

**通信方式**方面，MCP 支持本地通过 stdio 通信，也支持远程通过 HTTP+SSE（Server-Sent Events）通信，适配不同的部署场景。

它之所以在 agent 领域很受关注，是因为随着 AI agent 越来越需要"动手做事"而不只是"聊天回答"，一个标准化的工具连接协议变得非常关键。MCP 正在成为这个方向上的事实标准之一。
# rag

RAG（Retrieval-Augmented Generation，检索增强生成）是一种将**信息检索**与**大语言模型生成**相结合的技术框架。

核心思路很简单：在让语言模型回答问题之前，先从外部知识库中检索相关文档，把检索到的内容作为上下文一起喂给模型，让模型基于这些真实资料来生成回答。

整个流程大致分三步：

1. **索引（Indexing）**——把文档切分成小块，用 embedding 模型转成向量，存入向量数据库。
2. **检索（Retrieval）**——用户提问时，把问题也转成向量，在向量库中找到语义最相近的文档片段。
3. **生成（Generation）**——把检索到的片段和用户问题一起送进大语言模型，模型据此生成最终回答。

RAG 解决的核心痛点是：大模型的训练数据有截止日期，且无法直接访问私有数据。通过检索这一步，模型可以利用最新的、专属的知识来回答，同时减少"幻觉"（编造事实）的问题。

相比直接微调模型，RAG 的优势在于成本更低、知识更新更灵活——只需要更新知识库，不用重新训练模型。它在企业知识问答、客服系统、文档助手等场景中应用非常广泛。

# langchain

LangChain 是一个开源框架，用于构建基于大语言模型（LLM）的应用程序。它提供了一套工具和抽象层，帮助开发者更方便地将 LLM 集成到各种应用场景中。

**核心概念：**

LangChain 的设计理念是将 LLM 的调用与外部数据源、工具和记忆系统"链式"连接起来，从而构建更复杂、更实用的 AI 应用。主要模块包括：

**Models** — 统一接口调用不同的 LLM（如 OpenAI、Anthropic 等），方便切换和比较模型。

**Prompts** — 提供 prompt 模板管理，支持动态变量注入，便于复用和组织提示词。

**Chains** — 将多个步骤串联成一个工作流。比如先检索文档，再用 LLM 生成回答。

**Agents** — 让 LLM 自主决定使用哪些工具、按什么顺序执行任务，具备一定的"推理-行动"能力。

**Retrieval (RAG)** — 支持从向量数据库、文档等外部知识源中检索信息，实现检索增强生成。

**Memory** — 为对话提供上下文记忆，让多轮对话更连贯。

**典型应用场景：**

LangChain 常用于构建聊天机器人、文档问答系统、代码助手、自动化工作流、数据分析代理等。

**生态：**

LangChain 主要支持 Python 和 JavaScript/TypeScript，社区活跃，围绕它还有 LangSmith（调试和监控平台）和 LangGraph（构建有状态多步 agent 的框架）等配套工具。