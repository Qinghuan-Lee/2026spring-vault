Redis（**Re**mote **Di**ctionary **S**erver）是一个开源的内存数据结构存储系统，可以用作数据库、缓存和消息中间件。

它的核心特点包括：

**数据存储在内存中**，读写速度极快，通常能达到每秒数十万次操作。同时支持将数据持久化到磁盘（RDB 快照和 AOF 日志两种方式），防止数据丢失。

**支持丰富的数据结构**，不只是简单的键值对，还包括字符串（String）、哈希（Hash）、列表（List）、集合（Set）、有序集合（Sorted Set）、位图（Bitmap）、HyperLogLog、Stream 等。

**典型使用场景**有：缓存热点数据以减轻数据库压力、会话管理（Session）、排行榜（利用有序集合）、消息队列、分布式锁、实时计数器等。

**其他亮点**：支持主从复制、哨兵（Sentinel）高可用、集群（Cluster）分片，以及发布/订阅（Pub/Sub）功能，单线程模型配合 I/O 多路复用保证了高并发下的性能和数据一致性。

简单来说，Redis 就是一个"超快的、功能丰富的内存数据库"，在现代后端架构中几乎无处不在。